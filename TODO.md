# HarmonyOS WireGuard 客户端 — 待办清单

> 基于 `FEATURE.md` 和 `UI_DESIGN.md` 梳理，与当前代码实现对比后得出。

---

## P0：核心连接能力（阻塞上线）

### VPN 权限处理
- [ ] 首页检测 VPN 权限状态，未授权时显示权限申请引导卡片
- [ ] 系统 VPN 被其他应用占用时的冲突提示
- [ ] VPN Extension 启动失败时的错误捕获与提示

### 配置导入（当前为占位符）
- [ ] **相机扫描二维码**：集成 HarmonyOS 扫码 API (`scanBarcode`)
- [ ] **本地二维码图片导入**：从相册选择图片并解析二维码
- [ ] **.conf 文件导入**：集成文件选择器 (`documentViewPicker`)
- [ ] **剪贴板导入**：读取系统剪贴板文本并解析

### 连接状态机完善
- [ ] `disconnecting` 状态的支持
- [ ] `permissionRequired` 状态的支持
- [ ] 启动隧道前完整配置校验（当前部分校验已做）
- [ ] 启动失败后明确错误分类（权限/DNS/Endpoint/密钥）

---

## P1：配置管理与安全

### 配置导出
- [ ] 单配置导出为 `.conf` 文件
- [ ] 复制配置为文本
- [ ] 生成二维码图片
- [ ] 保存二维码图片到相册
- [ ] **敏感导出二次确认弹窗**（当前 UI 有设置项，缺少实际拦截逻辑）
- [ ] 批量导出 ZIP

### 私钥安全体系
- [ ] **私钥安全存储**：使用 HUKS 或 `userAuth` + `keyStore` 替代明文 Preferences
- [ ] 私钥查看二次确认弹窗（当前 UI 有组件，缺少触发逻辑）
- [ ] 私钥查看 30 秒后自动隐藏
- [ ] 应用进入后台时自动隐藏私钥
- [ ] 私钥复制后提示"请及时清空剪贴板"

### 剪贴板保护
- [ ] 复制敏感字段后延迟清空剪贴板（Settings 有开关，缺少实际逻辑）
- [ ] 粘贴配置后提示是否清除剪贴板

### 配置编辑增强
- [ ] Endpoint 拆分为 Host + Port 双输入框（当前为单文本框）
- [ ] 路由模式"自定义"时支持 CIDR Token 输入（当前仅支持文本输入）
- [ ] 配置重命名功能
- [ ] 配置复制功能
- [ ] 配置图标/颜色选择（UI_DESIGN 有设计，当前未实现）

---

## P2：状态监控与诊断

### 诊断体系（当前为占位符/模拟数据）
- [ ] **真实诊断逻辑**：`DiagnosticService.runDiagnostics()` 实际实现
  - [ ] VPN 权限检查
  - [ ] Endpoint DNS 解析检查
  - [ ] Endpoint UDP 可达性探测
  - [ ] 最近握手状态检查
  - [ ] DNS 生效检查
  - [ ] 默认路由是否进入 VPN 检查
  - [ ] IPv6 泄漏检查
  - [ ] 本地网段可达性检查
- [ ] 诊断规则化识别：有发送无接收 / 有握手无流量 / 无握手 / DNS 失败 / 内网不可达
- [ ] 诊断报告导出（当前按钮为占位符）
  - [ ] 导出日志脱敏
  - [ ] 导出运行状态摘要
  - [ ] 自动隐藏私钥和完整公网 IP

### 日志体系（当前为占位符）
- [ ] **真实日志记录**：`LogService.addLog()` 在各关键节点调用
  - [ ] App 启动/前后台切换
  - [ ] VPN Extension 启动/停止
  - [ ] 隧道启动/停止/失败
  - [ ] 配置导入/保存/解析失败
  - [ ] 网络切换/自动重连
- [ ] 日志等级过滤（Error/Warning/Info/Debug）
- [ ] 日志脱敏（PrivateKey/PresharedKey/完整配置原文不入日志）

### 流量监控
- [ ] **真实流量数据接入**：从 Native 层 `wgNative` 读取 rx/tx bytes
- [ ] 实时速率计算（当前为模拟数据）
- [ ] 流量图表 Canvas 绘制优化（当前 TrafficChart 可能为占位符）
- [ ] 页面不可见时暂停刷新

---

## P3：多端适配与性能

### 多端布局
- [ ] **平板双栏布局**：左侧配置列表 + 右侧详情（当前只有手机单栏）
- [ ] **PC/大屏三栏布局**：侧边栏 + 详情 + 诊断日志常驻
- [ ] 折叠屏横竖屏适配
- [ ] 紧凑模式开关生效（Settings 有开关，缺少实际布局切换）

### 深色模式
- [ ] `dark/element/color.json` 已存在，但缺少跟随系统/手动切换逻辑
- [ ] `SettingsPage` 中"跟随系统深色模式"开关实际生效

### 性能优化
- [ ] 配置列表使用 `LazyForEach` 替代 `ForEach`（配置多时长列表性能）
- [ ] 日志列表虚拟滚动或分页
- [ ] 状态刷新频率控制（连接状态事件驱动 + 1s 兜底）

---

## P4：自动化与高级功能

### 自动连接
- [ ] App 启动后自动连接上次隧道
- [ ] 系统重启后恢复连接（需后台任务/WorkScheduler）
- [ ] 网络切换后自动重连（监听网络变化事件）
- [ ] 连接失败后指数退避重试

### 按网络规则连接
- [ ] 连接指定 WLAN SSID 时自动连接/断开
- [ ] 可信网络规则配置页
- [ ] 离开可信网络时自动连接

### Kill Switch / 防泄漏
- [ ] VPN 断开时阻止流量（需系统能力支持）
- [ ] IPv6 防泄漏实际生效（Settings 有开关，缺少底层拦截）
- [ ] DNS 仅走 VPN

### 快捷控制
- [ ] 通知栏显示当前 VPN 状态（Foreground Service 通知）
- [ ] 通知栏一键断开
- [ ] 控制中心快捷开关（需系统支持）
- [ ] 长按 App 图标快速连接（需配置 Quick Action）

---

## 组件与交互补全

### 列表交互
- [ ] 隧道配置项长按弹出菜单（编辑/复制/导出/删除）
- [ ] 隧道配置项左滑露出操作按钮
- [ ] 下拉刷新配置列表状态
- [ ] 按下卡片 scale 0.98 动效

### 空状态
- [ ] 无配置时首页展示完整空状态（当前 EmptyState 组件存在，需确认集成完整）

### 表单体验
- [ ] 保存时滚动到第一个错误字段
- [ ] 输入框边框变红 + 下方错误文本

---

## 代码架构债务

### 模型层
- [ ] `WgConfig` 模型可扩展为 `TunnelProfile`（支持多 Peer、图标、颜色、行为配置）
- [ ] 当前模型为单 Peer，WireGuard 原生支持多 Peer

### 服务层
- [ ] `ConfigManager` 迁移到加密存储（HUKS）
- [ ] `TunnelRuntimeService` 与 Native 层真实数据对接
- [ ] `DiagnosticService` 真实诊断实现
- [ ] `LogService` 日志持久化（当前为内存存储）

### Native 层
- [ ] `WireGuardVpnAbility` 中 `wgNative.startVpn()` 实际参数校验
- [ ] Native 层返回运行态数据（rx/tx/lastHandshake）
- [ ] 错误码映射为中文用户提示

---

## 验收清单（来自 UI_DESIGN.md）

### UI 验收
- [ ] 首页打开后能立即看到当前连接状态
- [ ] 已连接/连接中/失败/未连接四种状态视觉明确
- [ ] 配置列表能区分全局代理和仅访问内网
- [ ] 配置编辑页字段分组清晰
- [ ] `AllowedIPs` 有路由模式解释
- [ ] 私钥默认隐藏
- [ ] 查看私钥有二次确认
- [ ] 导出配置有风险提示
- [ ] 深色模式可读
- [ ] 字体放大后布局不崩
- [ ] 手机、平板、PC 布局均可用

### WireGuard 功能验收
- [ ] 支持解析 `[Interface]` / `[Peer]`
- [ ] 支持多个 Address / DNS / AllowedIPs
- [ ] 支持 Endpoint 域名、IPv4、IPv6
- [ ] 支持 PersistentKeepalive / PresharedKey
- [ ] 能展示接收/发送流量（真实数据）
- [ ] 能展示最近握手（真实数据）
- [ ] 连接失败时能展示原因

---

*最后更新：2026-05-07*
